kind = "web"
previewPath = "/"
title = "Warehouse Camera Router"
version = "1.0.0"
id = "artifacts/warehouse-router"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21008

[services.development]
run = "pnpm --filter @workspace/warehouse-router run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/warehouse-router", "run", "build" ]
publicDir = "artifacts/warehouse-router/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21008"
BASE_PATH = "/"
